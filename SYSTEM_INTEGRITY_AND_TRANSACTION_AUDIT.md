SYSTEM INTEGRITY & TRANSACTION AUDIT
تدقيق سلامة المعاملات والمخزون والتوزيع والتحصيل
الهدف

هذا المستند يحدد اختبارات إلزامية للتأكد من أن النظام المالي ونظام المخزون والتوزيع يعملان بشكل ذرّي وآمن ومتسق، وأن أي عملية فاشلة أو متزامنة أو مكررة أو معدلة لا تؤدي إلى تضخيم الأرصدة أو الكميات أو إنشاء بيانات متناقضة.

القاعدة الأساسية:

لا يكفي أن تعمل العملية في الحالة الطبيعية.
يجب أن يبقى النظام صحيحًا أيضًا عند:

الضغط المتزامن.
الضغط المتكرر.
فشل العملية في منتصف التنفيذ.
انقطاع الاتصال.
إغلاق التطبيق.
تعديل السجلات القديمة.
الحذف.
تغيير التاريخ والوقت.
محاولة تجاوز الصلاحيات.
إرسال قيم غير صحيحة أو سالبة.
إعادة تنفيذ نفس العملية أكثر من مرة.
1. التزامن ومنع التكرار

يجب اختبار جميع العمليات الحساسة تحت التنفيذ المتزامن.

العمليات التي يجب اختبارها
تسجيل التحصيل.
إنشاء توزيع.
تسجيل مرتجع.
إنشاء حمولة.
تعديل توزيع.
تعديل مرتجع.
تعديل طلب.
حذف سجل.
حفظ سجل.
تحديث رصيد العميل.
تحديث المخزون.
الاختبارات
1.1 الضغط المزدوج

عند الضغط مرتين بسرعة على زر الحفظ:

Save → Save

يجب ألا يتم إنشاء سجلين.

النتيجة المطلوبة
عملية واحدة فقط.
1.2 الضغط المتزامن على أكثر من إجراء

اختبار:

حفظ + متابعة
حفظ + رجوع
حفظ + إغلاق
حفظ + فتح شاشة أخرى

يجب ألا يؤدي ذلك إلى تنفيذ العملية أكثر من مرة أو إنشاء حالة جزئية.

1.3 التنفيذ المتزامن

إذا تم إرسال نفس الطلب مرتين في نفس اللحظة، يجب أن يتعرف النظام على أنهما يمثلان نفس العملية أو يمنع التكرار.

ممنوع الاعتماد على تعطيل زر الواجهة فقط.

الحماية يجب أن تكون موجودة في طبقة البيانات / Repository / Transaction / Backend حسب معمارية النظام.

2. الحماية من التنفيذ المكرر Idempotency

كل عملية مالية أو مخزنية حساسة يجب أن تكون محمية من التنفيذ المكرر.

مثال:

تسجيل تحصيل 100

إذا تم إرسال نفس الطلب مرتين بسبب:

ضغط المستخدم.
بطء الشبكة.
إعادة المحاولة.
إعادة تشغيل التطبيق.

فلا يجوز أن تصبح النتيجة:

200

بل:

100

فقط.

3. المعاملات الذرية Atomic Transactions

أي عملية تتكون من أكثر من تحديث يجب أن تنفذ كعملية واحدة Atomic Transaction.

مثال تسجيل المرتجع:

1. إنشاء سجل المرتجع
2. تحديث كمية المخزون
3. تحديث رصيد العميل
4. تحديث الكمية المتاحة

يجب ألا يسمح النظام بأن تنجح خطوة وتفشل خطوة أخرى وتبقى البيانات في حالة جزئية.

السيناريو الإجباري

إذا حدث:

إنشاء المرتجع = نجح
تحديث المخزون = فشل

فالنتيجة يجب أن تكون:

ROLLBACK

أي:

لا يوجد مرتجع
ولا يوجد تحديث جزئي للمخزون
ولا يوجد تغيير جزئي في رصيد العميل
القاعدة

All or Nothing

إما تنجح المعاملة بالكامل، أو يتم التراجع عنها بالكامل.

4. اختبار التلاعب بالكميات

يجب منع جميع الكميات غير المنطقية.

4.1 التوزيع أكبر من الكمية المحملة

مثال:

الكمية المحملة = 100
الكمية الموزعة = 120

يجب رفض العملية.

4.2 المرتجع أكبر من التوزيع

مثال:

الموزع = 50
المرتجع = 60

يجب رفض العملية.

4.3 التالف أكبر من الكمية المتاحة

مثال:

المتاح = 20
التالف = 30

يجب رفض العملية.

4.4 القيم الصفرية والسالبة

اختبار:

كمية = 0
كمية = -1
كمية = -100

يجب تحديد ما إذا كانت العملية مسموحة تجاريًا أم لا، وعدم السماح بأي قيمة سالبة تؤدي إلى فساد المخزون.

5. قاعدة المخزون

يجب أن تكون هناك قاعدة واضحة ومصدر واحد للحقيقة بالنسبة للكميات.

لا يجوز أن يكون لدينا أكثر من مصدر مستقل يحسب الرصيد بطريقة مختلفة.

مثال:

الكمية المحملة
- التوزيع
+ المرتجع
- التالف
= الرصيد المتاح

يجب توثيق المعادلات الفعلية لكل نوع من أنواع المخزون.

يجب اختبار
الإضافة.
التعديل.
الحذف.
المرتجع.
التالف.
التوزيع.
الحمولة.

والتأكد من أن كل عملية تغير المخزون بالمقدار الصحيح فقط.

6. تعديل السجلات الحساسة

هذه من أخطر نقاط النظام.

المثال

السجل الأصلي:

التوزيع = 10

ثم قام المستخدم بتعديله إلى:

15

يجب ألا يقوم النظام بإضافة 15 إلى المخزون.

بل يحسب الفرق فقط:

15 - 10 = +5

وبالتالي يكون تأثير التعديل:

+5 فقط
المثال العكسي

السجل:

15

ثم أصبح:

8

يجب أن يكون تأثير التعديل:

8 - 15 = -7

أي يتم عكس الفرق فقط.

القاعدة

لا تعالج التعديل كعملية إضافة جديدة.

التعديل يجب أن يحسب:

New Value - Old Value

ثم يطبق الفرق فقط على الرصيد أو المخزون المتأثر.

7. التعديل المتكرر

يجب اختبار:

10 → 15 → 8 → 20 → 12

والتأكد أن النظام في كل مرة يحسب الفرق بين القيمة القديمة والجديدة، وليس القيمة الجديدة كاملة.

وفي النهاية يجب أن تكون جميع الأرصدة مساوية لما ينتج عن الحالة النهائية الفعلية للسجلات.

8. الحذف وتأثيره على البيانات

الحذف لا يعني فقط اختفاء السجل من الشاشة.

يجب اختبار التأثير الكامل للحذف.

8.1 حذف توزيع

بعد حذف التوزيع:

هل تعود كمية المخزون؟
هل يتغير رصيد العميل؟
هل تتغير الكمية المتاحة؟
هل تختفي النتائج التابعة له؟
هل التقارير أصبحت صحيحة؟
8.2 حذف مرتجع

بعد حذف المرتجع:

هل تعود الكمية إلى حالتها الصحيحة؟
هل يتغير المخزون؟
هل يتغير رصيد العميل إذا كان المرتجع يؤثر عليه؟
8.3 حذف طلب

بعد حذف الطلب:

هل يعود رصيد العميل؟
هل تتغير الكميات؟
هل تختفي آثاره من التقارير؟
8.4 حذف حمولة

بعد حذف الحمولة:

هل يتم عكس أثرها على المخزون؟
هل تختفي الكمية التي أضافتها؟
هل تتأثر العمليات التابعة لها بشكل صحيح؟
القاعدة

يجب ألا يؤدي حذف سجل إلى ترك أثر مالي أو مخزني يتيم.

9. Orphan Records

يجب البحث عن السجلات اليتيمة.

مثال:

Distribution → Customer
Return → Distribution
Payment → Customer
Load → Inventory

إذا تم حذف العنصر الأساسي، يجب تحديد مصير السجلات التابعة له.

ممنوع وجود سجل يشير إلى معرف غير موجود.

يجب اختبار:

Create
Update
Delete
Restore / Rebuild

والتحقق من سلامة العلاقات.

10. مصدر الحقيقة Single Source of Truth

يجب ألا تعتمد التقارير والأرصدة على قيم مخزنة في عدة أماكن يمكن أن تختلف عن بعضها.

مثال:

إذا كان:

رصيد العميل

موجودًا في أكثر من جدول أو Collection أو Document، فيجب تحديد:

من هو المصدر الأساسي؟
من يقوم بالتحديث؟
كيف تتم إعادة الحساب؟
ماذا يحدث عند فشل أحد التحديثات؟
الهدف

منع حالة مثل:

صفحة العميل = 100
التقرير = 80
الملخص = 120
11. التقريب والحسابات المالية

يجب اختبار الحسابات التي تحتوي على كسور عشرية.

أمثلة:

0.05
0.957575
0.1
0.3
0.7

يجب عدم الاعتماد على Floating Point بطريقة تؤدي إلى فروقات مالية غير مقصودة.

يجب تحديد:

وحدة الدقة.
عدد المنازل العشرية.
طريقة التقريب.
متى يتم التقريب.
هل يتم تخزين القيمة قبل التقريب أم بعده.
القاعدة

يجب أن تكون نتيجة العمليات الحسابية متسقة في:

العميل.
المخزون.
التوزيع.
المرتجع.
التحصيل.
التقارير.
12. إغلاق اليوم

يجب اختبار دورة اليوم بالكامل.

الأسئلة الإلزامية

هل يمكن إغلاق اليوم إذا كانت هناك كمية غير موزعة؟

هل يمكن إغلاق اليوم إذا كان هناك فرق في المخزون؟

هل يمكن إغلاق اليوم إذا كانت هناك عملية غير مكتملة؟

هل يمكن تعديل توزيع بعد إغلاق اليوم؟

هل يمكن إضافة مرتجع بتاريخ اليوم بعد إغلاق اليوم؟

هل يمكن حذف عملية من يوم مغلق؟

هل يمكن تعديل سجل مالي من يوم سابق؟

يجب أن تكون هذه القواعد محددة بوضوح في النظام.

13. العمليات بتاريخ قديم

اختبار إنشاء عملية بتاريخ سابق.

مثال:

اليوم:

04/08/2026

ثم إنشاء توزيع بتاريخ:

01/08/2026

يجب ألا يسمح النظام بذلك بشكل عشوائي.

يجب تحديد سياسة واضحة:

هل العملية بأثر رجعي مسموحة؟
إذا كانت مسموحة:
من يملك الصلاحية؟
هل تؤثر على الأرصدة الحالية؟
هل يعاد احتساب الأيام اللاحقة؟
هل تسجل في Audit Log؟
14. تعديل السجلات من أيام سابقة

يجب اختبار تعديل:

سجل من اليوم السابق
سجل من أسبوع سابق
سجل من يوم مغلق

ويجب ألا يؤدي التعديل إلى كسر أرصدة الأيام التالية.

15. التاريخ والوقت

لا يجب أن تكون سلامة النظام معتمدة على ساعة جهاز المستخدم فقط.

يجب اختبار:

23:59
00:01

والتحقق من انتقال اليوم بشكل صحيح.

كما يجب اختبار تغيير تاريخ ووقت الجهاز يدويًا.

مثال

المستخدم يغير تاريخ الجهاز من:

04/08

إلى:

01/08

ثم يحاول إنشاء عملية بتاريخ قديم.

يجب ألا يستطيع تجاوز قواعد النظام بهذه الطريقة.

16. الصلاحيات Authorization

يجب عدم الاعتماد على إخفاء الأزرار في الواجهة فقط.

مثال:

إذا كان المستخدم لا يملك صلاحية:

Delete Distribution

فلا يكفي إخفاء زر الحذف.

يجب أن يرفض النظام العملية نفسها على مستوى:

Repository
Service
Backend / Database Rules

بحسب بنية المشروع.

اختبار إلزامي

محاولة استدعاء عملية محمية مباشرة دون المرور بالواجهة.

النتيجة المطلوبة:

Permission Denied
17. تجاوز Repository / Service

يجب اختبار أن المستخدم أو العميل لا يستطيع تجاوز قواعد العمل عن طريق استدعاء طبقة البيانات مباشرة.

مثال:

إذا كانت القاعدة:

لا يمكن توزيع أكثر من الكمية المحملة

فيجب أن تبقى هذه القاعدة صحيحة حتى لو تم استدعاء Repository مباشرة.

Business Rules must not depend on UI validation.

18. انقطاع الاتصال

اختبار العمليات أثناء انقطاع الإنترنت أو فقد الاتصال بقاعدة البيانات.

السيناريو:

المستخدم يبدأ العملية
↓
يتم حفظ جزء من البيانات
↓
ينقطع الاتصال

يجب ألا تترك العملية النظام في حالة جزئية.

يجب اختبار:

انقطاع الشبكة أثناء الحفظ.
إغلاق التطبيق أثناء الحفظ.
إعادة فتح التطبيق.
إعادة المحاولة.
الضغط على حفظ مرة أخرى.
19. إعادة تشغيل التطبيق

بعد كل عملية حساسة:

Save
Close App
Open App

يجب التحقق من:

الرصيد.
المخزون.
التوزيع.
المرتجع.
التحصيل.
التقارير.

ويجب أن تكون قاعدة البيانات هي المصدر الحقيقي للحالة النهائية.

20. إعادة بناء الحالة Reconciliation

يجب توفير آلية يمكن من خلالها مقارنة:

Current Stored Balance

مع:

Balance Calculated From Transactions

مثال:

الرصيد الحالي = 80

ثم يعيد النظام الحساب من جميع العمليات:

100 - 30 + 10 = 80

يجب أن تكون النتيجة متطابقة.

إذا ظهرت:

Stored = 80
Calculated = 85

فيجب اعتبار ذلك Data Integrity Error وليس مجرد اختلاف عرض.

21. الاختبار بعد كل تعديل

بعد أي:

Create
Update
Delete
Return
Payment
Distribution
Load

يجب إعادة التحقق من:

Database
Inventory
Customer Balance
Reports
Daily Totals
Historical Totals
القاعدة النهائية

بعد كل عملية، يجب أن تكون قاعدة البيانات قابلة لإعادة بناء الحالة منها دون تناقض.

22. سجل التدقيق Audit Log

كل عملية حساسة يجب أن تكون قابلة للتتبع.

يجب معرفة:

من قام بالعملية؟
متى؟
ما العملية؟
ما القيمة القديمة؟
ما القيمة الجديدة؟
ما الفرق؟
ما السجل المتأثر؟

خصوصًا:

التعديل.
الحذف.
المرتجع.
التحصيل.
التوزيع.
تغيير المخزون.
العمليات بأثر رجعي.
23. الاختبار النهائي End-to-End

يجب تنفيذ سيناريو كامل من البداية إلى النهاية:

إنشاء حمولة
↓
تسجيل الكمية
↓
إنشاء توزيع
↓
تحديث المخزون
↓
تسجيل مرتجع
↓
تحديث المخزون
↓
تسجيل تحصيل
↓
تحديث رصيد العميل
↓
تعديل التوزيع
↓
إعادة حساب الفرق
↓
حذف المرتجع
↓
إعادة الرصيد
↓
إغلاق اليوم
↓
إعادة تشغيل التطبيق
↓
مراجعة التقارير

وفي كل مرحلة يجب التحقق من أن:

المخزون صحيح
+
رصيد العميل صحيح
+
التقارير صحيحة
+
السجلات مترابطة
+
لا توجد بيانات يتيمة
24. معايير النجاح

لا يعتبر النظام ناجحًا في هذا التدقيق لمجرد أن الواجهة تعمل.

يجب تحقيق جميع الشروط التالية:

لا توجد عمليات مكررة.
لا توجد معاملات جزئية.
لا توجد أرصدة سالبة غير مسموحة.
لا يوجد تضخيم للمخزون.
لا يوجد تضخيم للتحصيل.
تعديل السجل يحسب الفرق فقط.
الحذف يعكس أثر العملية بالكامل.
لا توجد سجلات يتيمة.
الصلاحيات مطبقة خارج الواجهة.
تغيير تاريخ الجهاز لا يتجاوز قواعد النظام.
إغلاق اليوم يحترم قواعد العمل.
العمليات القديمة لا تكسر الأيام اللاحقة.
الحسابات العشرية مستقرة.
إعادة تشغيل التطبيق لا تغير الحالة.
انقطاع الاتصال لا يترك بيانات جزئية.
قاعدة البيانات يمكن منها إعادة بناء الحالة الصحيحة.
التقارير متطابقة مع البيانات المصدرية.
25. تعليمات إلزامية للمطور

لا تقم فقط باختبار الواجهة.

قم بمراجعة النظام على مستوى:

UI
↓
Controller / State
↓
Service
↓
Repository
↓
Database
↓
Security Rules / Authorization

ويجب تحديد مكان كل قاعدة من قواعد العمل.

إذا كانت هناك قاعدة مهمة موجودة في الواجهة فقط، فيجب نقلها أو تكرار فرضها في الطبقة المناسبة بحيث لا يمكن تجاوزها باستدعاء مباشر.

ممنوع
UI Validation = Security
المطلوب
UI Validation
+
Business Validation
+
Transaction Safety
+
Authorization
+
Database Integrity
26. التقرير المطلوب من المطور

بعد الانتهاء من التدقيق، لا تكتفِ بقول:

تم الاختبار.

يجب إنشاء تقرير يحتوي على:

الاختبار	النتيجة	الحالة	الملاحظة	الإصلاح
الضغط المزدوج	...	PASS/FAIL	...	...
التوزيع أكبر من الحمولة	...	PASS/FAIL	...	...
المرتجع أكبر من التوزيع	...	PASS/FAIL	...	...
تعديل 10 → 15	...	PASS/FAIL	...	...
تعديل 15 → 8	...	PASS/FAIL	...	...
حذف التوزيع	...	PASS/FAIL	...	...
حذف المرتجع	...	PASS/FAIL	...	...
انقطاع الاتصال	...	PASS/FAIL	...	...
إعادة تشغيل التطبيق	...	PASS/FAIL	...	...
تغيير تاريخ الجهاز	...	PASS/FAIL	...	...
إغلاق اليوم	...	PASS/FAIL	...	...
الصلاحيات	...	PASS/FAIL	...	...
القاعدة الذهبية للنظام

أي عملية مالية أو مخزنية يجب أن تكون:

VALIDATED
+
AUTHORIZED
+
ATOMIC
+
IDEMPOTENT
+
AUDITABLE
+
REVERSIBLE

والأهم:

لا نريد نظامًا يبدو صحيحًا على الشاشة؛ نريد قاعدة بيانات لا يمكن أن تدخل في حالة متناقضة حتى عند حدوث الخطأ أو التزامن أو التعديل أو الحذف أو انقطاع الاتصال.


المزامنة ليست عملية نسخ بيانات من الجهاز إلى السحابة. المزامنة جزء من النظام المالي نفسه، ويجب أن تحافظ على صحة المعاملات والأرصدة والمخزون مهما كان ترتيب الاتصال أو انقطاعه أو تكرار المحاولة أو تعدد الأجهزة.

27. المزامنة السحابية وسلامة البيانات
CLOUD SYNC & OFFLINE DATA INTEGRITY AUDIT

النظام يحتوي على:

Local Database
        ↕
Synchronization Layer
        ↕
Cloud Database

لذلك يجب اعتبار المزامنة جزءًا من منطق النظام المالي والمخزني وليست مجرد نقل بيانات.

القاعدة الأساسية

لا يجوز أن تؤدي المزامنة إلى إنشاء عملية مكررة، أو فقدان عملية صحيحة، أو الكتابة فوق تعديل أحدث، أو اختلاف الأرصدة بين الجهاز والسحابة.

28. العمل بدون اتصال Offline

يجب اختبار النظام بالكامل أثناء عدم وجود الإنترنت.

مثال:

Offline
↓
إنشاء توزيع
↓
تسجيل مرتجع
↓
تسجيل تحصيل
↓
تعديل سجل
↓
إغلاق التطبيق
↓
فتح التطبيق
↓
عودة الإنترنت
↓
Sync

بعد عودة الاتصال يجب أن تنتقل جميع العمليات الصحيحة إلى السحابة مرة واحدة فقط.

29. المزامنة بعد انقطاع الإنترنت

اختبار:

إنشاء عملية
↓
فقد الإنترنت أثناء المزامنة
↓
عودة الإنترنت

يجب ألا ينتج:

Duplicate Transaction

ولا:

Partial Transaction

ولا:

Missing Transaction
30. المزامنة المتكررة

يجب اختبار:

Sync
Sync
Sync
Sync

عدة مرات.

يجب أن تكون النتيجة نفسها بعد كل مرة.

أي أن تشغيل المزامنة عدة مرات لا يضاعف:

التوزيعات.
المرتجعات.
التحصيلات.
الكميات.
الأرصدة.
31. المزامنة المكررة Idempotency

إذا أرسل الجهاز نفس العملية إلى السحابة مرتين بسبب:

ضعف الشبكة.
Timeout.
Retry.
إغلاق التطبيق.
إعادة فتح التطبيق.

فيجب أن تتعرف السحابة على أنها نفس العملية.

يجب أن يكون لكل عملية معرف فريد ثابت، مثل:

transactionId
operationId
UUID

ولا يجوز إنشاء معرف جديد للعملية نفسها عند كل محاولة مزامنة.

32. التزامن بين جهازين

هذا اختبار بالغ الأهمية.

مثال:

الجهاز A
توزيع = 10

الجهاز B
توزيع = 5

ثم كلا الجهازين يعملان بدون اتصال.

بعد ذلك:

A → Cloud
B → Cloud

يجب ألا يؤدي ذلك إلى فقد إحدى العمليتين.

إذا كانت الكمية المتاحة:

10

فلا يجوز أن تسمح المزامنة بظهور:

15

إذا كان مجموع التوزيعات يتجاوز الكمية المتاحة.

33. تعديل نفس السجل من جهازين

مثال:

السجل الأصلي:

التوزيع = 10

الجهاز A يعدله إلى:

15

والجهاز B يعدله إلى:

8

ثم تتم المزامنة.

يجب ألا يختار النظام النتيجة عشوائيًا.

يجب وجود سياسة واضحة لـ:

Conflict Resolution

مثل:

Last Write Wins إذا كان مناسبًا.
Version Number.
Optimistic Concurrency.
Conflict Detection.
أو منع التعديل عند وجود إصدار أحدث.

المطور يجب أن يوثق السياسة المستخدمة فعليًا.

34. Versioning

السجلات الحساسة يجب أن تحتوي، حسب التصميم المناسب، على آلية لمعرفة إصدار السجل.

مثال:

version = 5

إذا حاول جهاز إرسال تعديل مبني على:

version = 4

بينما السحابة أصبحت:

version = 5

فلا يجب أن يكتب فوق البيانات الجديدة بشكل صامت.

بل يجب:

Detect Conflict
→ Reject / Resolve

بحسب سياسة النظام.

35. الحذف والمزامنة

هذه نقطة خطيرة جدًا.

إذا حذف الجهاز A سجلًا أثناء Offline:

Delete Distribution X

بينما الجهاز B لا يزال يحتوي على السجل:

Distribution X

ثم تمت المزامنة.

لا يجوز أن يعود السجل تلقائيًا بسبب مزامنة الجهاز B.

لذلك يجب أن تكون عمليات الحذف قابلة للتتبع في المزامنة.

يفضل استخدام مفهوم مثل:

Tombstone / Deleted Marker

أو أي آلية مكافئة مناسبة للتصميم.

36. تعديل ثم حذف أثناء Offline

يجب اختبار:

10
↓
تعديل إلى 15
↓
حذف
↓
Sync

والتأكد من عدم ظهور السجل مرة أخرى في السحابة أو الجهاز.

37. إنشاء ثم حذف قبل المزامنة

اختبار:

Create Distribution
↓
Delete Distribution
↓
Sync

يجب ألا يؤدي ذلك إلى ظهور التوزيع في السحابة.

38. ترتيب العمليات

يجب اختبار العمليات التي تحدث Offline بترتيب زمني:

Create
↓
Update
↓
Update
↓
Delete

ويجب أن تصل السحابة إلى الحالة النهائية الصحيحة.

ولا يجوز إرسال العمليات بترتيب يؤدي إلى حالة غير صحيحة.

39. فشل مزامنة جزء من العمليات

مثال:

الجهاز لديه:

100 عملية غير متزامنة

تمت مزامنة:

1 → 60

ثم فشلت:

61 → 100

يجب أن يحتفظ النظام بحالة واضحة:

Synced
Pending
Failed
Retry

ولا يجوز اعتبار العمليات 61–100 ناجحة قبل نجاحها فعليًا.

40. استئناف المزامنة

بعد فشل المزامنة:

Sync Failed

ثم عودة الإنترنت.

يجب أن يستطيع النظام:

Resume / Retry

دون إعادة إرسال العمليات الناجحة بطريقة تؤدي إلى التكرار.

41. المزامنة والرصيد

يجب اختبار حالة مهمة جدًا:

الجهاز Offline لديه:

رصيد العميل = 100

تم تسجيل تحصيل:

20

فيصبح محليًا:

80

ثم تتم المزامنة.

يجب أن تصبح السحابة:

80

وليس:

100

أو:

60

بسبب تطبيق العملية مرتين.

42. المزامنة والمخزون

مثال:

السحابة:

Stock = 100

الجهاز Offline:

Distribution = 20

محليًا:

Stock = 80

بعد المزامنة يجب أن تكون السحابة:

Stock = 80

ويجب ألا يتم:

100 → 80 → 60

بسبب إعادة تطبيق العملية.

43. لا تعتمد المزامنة على القيم النهائية فقط

يجب تجنب تصميم مثل:

Cloud Stock = Local Stock

بشكل أعمى.

خصوصًا في البيانات المالية والمخزنية.

لأن جهازين قد يحتويان على تعديلات مختلفة.

يجب تحديد هل تتم مزامنة:

Events / Transactions

أم:

Final State

أم مزيج منهما.

ويجب أن تكون السياسة موحدة وواضحة.

44. مصدر الحقيقة بعد المزامنة

يجب تحديد بشكل صريح:

Local Database
Cloud Database

من هو Source of Truth في كل حالة؟

مثلاً:

أثناء Offline
Local = Source of Truth مؤقتًا
بعد المزامنة الناجحة
Cloud = المصدر المركزي

لكن يجب ألا يؤدي ذلك إلى فقدان عمليات محلية صحيحة لم تتم مزامنتها بعد.

45. إعادة تثبيت التطبيق / جهاز جديد

اختبار مهم:

Cloud contains data
↓
تسجيل الدخول من جهاز جديد
↓
Initial Sync

يجب التأكد من تنزيل جميع البيانات الصحيحة.

ثم:

Local Data
vs
Cloud Data

ويجب ألا توجد فروقات غير مبررة.

46. إعادة بناء قاعدة البيانات المحلية

اختبار:

Delete Local Database
↓
Login
↓
Full Sync From Cloud

بعد المزامنة يجب أن تعود الحالة إلى نفس الحالة السابقة:

Customers
Inventory
Distributions
Returns
Payments
Reports
Balances

وهذه من أقوى اختبارات سلامة النظام.

47. إعادة بناء الحالة السحابية

يجب أن يكون من الممكن التحقق من صحة البيانات السحابية من خلال العمليات الأصلية.

مثلاً:

Loads
- Distributions
+ Returns
- Damaged
= Expected Inventory

ثم مقارنة:

Expected Inventory
vs
Stored Inventory

إذا اختلفا، فهذا يعني وجود مشكلة في سلامة البيانات.

48. التعارضات Conflict Management

يجب ألا تكون معالجة التعارضات:

آخر جهاز كتب = الفائز

بشكل تلقائي في كل الحالات.

خصوصًا في:

التحصيل.
المخزون.
التوزيع.
المرتجع.
الحذف.
تعديل السجلات المالية.

يجب التفريق بين:

State Conflict

تعديل نفس السجل.

و:

Transaction Conflict

عمليتان مستقلتان تؤثران على نفس الرصيد أو المخزون.

وهما ليستا المشكلة نفسها.

49. Audit Log للمزامنة

يجب تسجيل:

Created Locally
Synced
Sync Failed
Retry
Conflict
Resolved
Deleted

بحيث يمكن معرفة مسار العملية.

مثال:

Transaction ID: XYZ
Created: Local
Status: Pending
Sync Attempt: 1 → Failed
Sync Attempt: 2 → Success
50. الاختبار النهائي للمزامنة

يجب تنفيذ السيناريو التالي:

الجهاز A Offline
        ↓
إنشاء توزيع 10
        ↓
تعديل إلى 15
        ↓
تسجيل مرتجع 3
        ↓
تسجيل تحصيل
        ↓

الجهاز B Offline
        ↓
إنشاء توزيع آخر
        ↓
تعديل سجل قديم
        ↓

عودة الإنترنت للجهاز A
        ↓
Sync
        ↓

عودة الإنترنت للجهاز B
        ↓
Sync
        ↓

Conflict Detection
        ↓
Resolution
        ↓

إعادة تشغيل الجهازين
        ↓
مقارنة البيانات
        ↓

مقارنة Local
vs
Cloud
vs
Calculated State
النتيجة النهائية المطلوبة

يجب أن تكون:

Local A
   =
Local B
   =
Cloud
   =
Calculated State

مع مراعاة العمليات التي لا تزال Pending إن وجدت.

51. معيار النجاح النهائي

لا يعتبر النظام آمنًا للإنتاج إلا إذا أثبت أن:

Offline + Online + Sync + Retry + Concurrent Operations + Conflicts + Delete + Update + Failure

لا تؤدي إلى:

Duplicate
Lost Data
Negative Stock
Incorrect Balance
Partial Transaction
Orphan Record
Unauthorized Modification
Silent Conflict